---
import type { VisitPageData, PricingFeatureGroup } from "../../types/visits";
import PricingSection from "../patterns/PricingSection.astro";

interface Props {
  pricing: VisitPageData["pricing"] & { plans: PricingFeatureGroup[] };
}

const { pricing } = Astro.props;
---

<PricingSection pricing={pricing} />
