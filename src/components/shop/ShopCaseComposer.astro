---
import { getImage, Image } from "astro:assets";
import type { ShopPageData } from "../../types/shop";
import ActionLink from "../ui/actions/ActionLink.astro";

interface Props {
  caseComposer: ShopPageData["caseComposer"];
}

const { caseComposer } = Astro.props;
const composerBg = await getImage({ src: caseComposer.backdropImage, format: "webp", quality: 80, width: 1920 });
---

<section
  id="coffrets"
  class="shop-composer section"
  aria-labelledby="shop-composer-title"
  style={`--shop-composer-bg:url('${composerBg.src}')`}
>
  <div class="shop-composer__backdrop" aria-hidden="true"></div>
  <div class="shell shop-shell shop-composer__shell">
    <figure class="shop-composer__crate ui-hover-media" data-reveal="left">
      <Image src={caseComposer.crateImage} alt={caseComposer.crateImageAlt} loading="lazy" />
    </figure>

    <div class="shop-composer__content" data-reveal-group data-reveal-stagger="80">
      <p class="shop-composer__eyebrow" data-reveal="up">{caseComposer.eyebrow}</p>
      <h2 id="shop-composer-title" data-reveal="up">{caseComposer.title}</h2>
      <p class="shop-composer__intro" data-reveal="up">{caseComposer.intro}</p>

      <div class="shop-composer__offers" data-reveal-group data-reveal-stagger="70">
        {caseComposer.offers.map((offer) => (
          <article class="shop-offer ui-hover-lift" data-reveal="up">
            <h3>{offer.title}</h3>
            {offer.saving && <p class="shop-offer__saving">{offer.saving}</p>}
            <ul>
              {offer.lines.map((line) => (
                <li>{line}</li>
              ))}
            </ul>
            <div class="shop-offer__price">
              <p>{offer.price}</p>
              {offer.strikePrice && <span>{offer.strikePrice}</span>}
            </div>
            <p class="shop-offer__details">{offer.details}</p>

            <ActionLink class="shop-offer__cta" href={offer.ctaHref} label={offer.ctaLabel} variant="secondary" size="md" />
          </article>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .shop-composer {
    background: #121212;
    overflow: clip;
    padding-block: clamp(2.3rem, 4vw, 3.4rem);
    position: relative;
  }

  .shop-composer__backdrop {
    background:
      linear-gradient(180deg, rgba(18, 18, 18, 0.82), rgba(18, 18, 18, 0.94)),
      var(--shop-composer-bg) center / cover no-repeat;
    inset: 0;
    position: absolute;
  }

  .shop-composer__shell {
    align-items: end;
    display: grid;
    gap: 2rem;
    grid-template-columns: minmax(0, 0.92fr) minmax(0, 1fr);
    position: relative;
    z-index: 1;
  }

  .shop-composer__crate {
    border-radius: 1.25rem;
    margin: 0;
    overflow: hidden;
  }

  .shop-composer__crate img {
    aspect-ratio: 0.8;
    height: 100%;
    object-fit: cover;
    object-position: center 30%;
    width: 100%;
  }

  .shop-composer__content {
    color: rgba(255, 255, 255, 0.84);
    padding-bottom: 0.2rem;
  }

  .shop-composer__eyebrow {
    color: var(--color-accent-text);
    font-size: 0.78rem;
    letter-spacing: 0.2em;
    margin: 0;
    text-transform: uppercase;
  }

  .shop-composer h2 {
    color: var(--color-white);
    font-size: clamp(1.8rem, 3.5vw, 2.8rem);
    margin: 0.55rem 0;
  }

  .shop-composer__intro {
    color: rgba(255, 255, 255, 0.72);
    line-height: 1.45;
    margin: 0;
    max-width: 30rem;
  }

  .shop-composer__offers {
    display: grid;
    gap: 0.8rem;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin-top: 0.95rem;
  }

  .shop-offer {
    background: rgba(255, 255, 255, 0.09);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 1.25rem;
    padding: 1.05rem;
  }

  .shop-offer h3 {
    color: var(--color-white);
    font-size: 1.25rem;
    margin: 0;
  }

  .shop-offer__saving {
    color: var(--color-cta);
    font-size: 0.77rem;
    letter-spacing: 0.12em;
    margin: 0.45rem 0 0;
    text-transform: uppercase;
  }

  .shop-offer ul {
    display: grid;
    gap: 0.15rem;
    list-style: none;
    margin: 0.65rem 0 0;
    padding: 0;
  }

  .shop-offer li {
    color: rgba(255, 255, 255, 0.84);
    font-size: 0.95rem;
    line-height: 1.5;
    padding-left: 1rem;
    position: relative;
  }

  .shop-offer li::before {
    color: var(--color-accent-text);
    content: "›";
    left: 0;
    position: absolute;
  }

  .shop-offer__price {
    align-items: center;
    display: flex;
    gap: 0.65rem;
    margin-top: 0.6rem;
  }

  .shop-offer__price p {
    color: var(--color-white);
    font-family: var(--font-display);
    font-size: 1.65rem;
    margin: 0;
  }

  .shop-offer__price span {
    color: rgba(255, 255, 255, 0.66);
    font-size: 0.98rem;
    text-decoration: line-through;
  }

  .shop-offer__details {
    color: rgba(255, 255, 255, 0.66);
    font-size: 0.84rem;
    margin: 0.05rem 0 0;
  }

  .shop-offer :global(.shop-offer__cta) {
    --action-secondary-bg: transparent;
    --action-secondary-bg-hover: rgba(214, 116, 0, 0.14);
    --action-secondary-border: var(--color-cta);
    --action-secondary-color: var(--color-cta);

    margin-top: 0.75rem;
    min-height: 2.7rem;
    padding: 0 1.2rem;
  }

  @media (max-width: 1023px) {
    .shop-composer__shell {
      align-items: start;
      grid-template-columns: 1fr;
    }

    .shop-composer__crate {
      max-height: 22rem;
    }

    .shop-composer__crate img {
      aspect-ratio: 1.8;
    }

    .shop-composer__offers {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 767px) {
    .shop-offer {
      padding: 1.1rem;
    }

    .shop-offer :global(.shop-offer__cta) {
      justify-content: center;
      width: 100%;
    }
  }
</style>
